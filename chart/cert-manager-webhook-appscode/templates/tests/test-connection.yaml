apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cert-manager-webhook-appscode.fullname" . }}-test-connection"
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "cert-manager-webhook-appscode.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "cert-manager-webhook-appscode.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
